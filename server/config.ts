export const RESOURCE_NODE_CAP = 80; // Global hard cap on total stored resource nodes

// Limits for logs and lists
export const MAX_EVENT_LOG_MESSAGES = 200; // Keep only the most recent 200 global events
export const MAX_EVENT_MESSAGE_LEN = 240; // Clamp event message length to 240 characters
export const MAX_NOTIFICATION_ENTRIES = 10; // Keep only the most recent 10 notifications per player
